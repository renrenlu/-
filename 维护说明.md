# 维护说明

## 文件分工

- [index.html](/Users/ren/Documents/可交互网页/index.html)：页面结构、模块顺序、标题文案
- [styles.css](/Users/ren/Documents/可交互网页/styles.css)：主题变量、排版、卡片样式、移动端布局
- [script.js](/Users/ren/Documents/可交互网页/script.js)：月历渲染、提醒、个人日程、倒计时、主题切换
- [calendar-data.js](/Users/ren/Documents/可交互网页/calendar-data.js)：节日、节气文案、法定节假日数据
- [user-data-store.js](/Users/ren/Documents/可交互网页/user-data-store.js)：本机持久化，优先 `IndexedDB`，兼容 `localStorage`
- [service-worker.js](/Users/ren/Documents/可交互网页/service-worker.js)：离线缓存

## 最常改的地方

### 1. 改颜色和标题

去 [styles.css](/Users/ren/Documents/可交互网页/styles.css) 顶部 `:root` 变量区：

- 品牌色：`--accent`、`--accent-deep`
- 标题色：`--title-ink`
- 顶部渐变：`--hero-start`、`--hero-mid`、`--hero-end`
- 标题字号：`--title-hero-size`、`--title-section-size`、`--title-card-size`、`--title-detail-size`
- 字体：`--font-title`、`--font-display`

原则：

- 想统一改样式，先改变量，不要先改单个模块
- 想改整页标题观感，优先改 `--title-*`

### 2. 改模块顺序

去 [index.html](/Users/ren/Documents/可交互网页/index.html)。

目前页面大结构是：

1. 顶部主视觉 `hero`
2. 快捷信息 `quick-strip`
3. 日历区 `calendar-section`
4. 工具区 `tools-section`
5. 底部导航 `app-dock`

如果要把某块挪位置，优先移动整个 section，不要只改视觉间距。

### 3. 改节假日和节气内容

去 [calendar-data.js](/Users/ren/Documents/可交互网页/calendar-data.js)。

主要维护这些数据：

- `philosophyQuotes`
- `solarFestivals`
- `solarTermMeta`
- `holidayPlans`
- `officialSources`

建议：

- 新增法定假期时，按年份继续补到 `holidayPlans`
- 同时在 `officialSources` 里补来源信息

### 4. 改提醒和个人日程逻辑

去 [script.js](/Users/ren/Documents/可交互网页/script.js)。

最常用入口：

- `renderAll()`：总渲染入口
- `renderCalendar()`：月历
- `renderSelectedDetail()`：当天详情
- `renderReminders()`：提醒列表
- `renderSchedules()`：个人日程
- `handleReminderSubmit()`：添加提醒
- `handleScheduleSubmit()`：添加日程

建议：

- 加新功能时，优先接到现有 `render*` / `handle*` 结构里
- 不要把新逻辑散落到多个匿名事件里，后面不好找

### 5. 改本机保存

去 [user-data-store.js](/Users/ren/Documents/可交互网页/user-data-store.js)。

当前策略：

- 优先使用 `IndexedDB`
- 不支持时自动降级到 `localStorage`

现在已经保存：

- 主题
- 提醒
- 个人日程
- 当前查看日期和月份

如果以后要接账号同步，优先保留这个本地层，再往上接云端接口。

## 推荐维护顺序

1. 先改变量和文案
2. 再看结构需不需要调整
3. 最后再动交互逻辑

这样最不容易把页面改乱。
